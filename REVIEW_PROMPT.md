# AI 代码 Review Prompt 模板

---

## 普通版（日常功能）

```
你刚才写完了代码，现在对照下面的检查清单，做一次自我 review。

【本次改动】
- 改动模块：___
- 改动内容：___
- 涉及分类/类型：是 / 否
- 涉及时间逻辑：是 / 否
- 涉及分页/排行榜：是 / 否

【要求】
逐项回答，不能只说"已检查"，要说明你是怎么处理的，或者为什么不涉及。
如果有风险，直接说，不要美化。

【检查清单】

公共逻辑：
- 有没有修改公共组件或公共函数？如果有，其他调用方是否受影响？
- 有没有把某个分类的特殊逻辑改进了公共逻辑？

分类/类型：
- 新增或修改的逻辑，是否通过分类 ID/类型字段做了条件判断？
- 其他分类的上传规则、表单提示、审核流程是否未受影响？

权限：
- 是否有新增接口？新接口是否有权限校验？
- 前端隐藏入口的同时，后端是否也做了拦截？

表单/提交：
- 重复提交是否有防护？
- 提交失败后按钮是否恢复可点击？

状态：
- 是否有遗漏的状态判断（如已取消、已关闭）？
- 状态切换后页面 UI 是否同步更新？

字段来源一致性：
- 同一个业务含义在列表页、详情页、卡片组件、提交/回显页是否取自同一个数据源？
- 如果字段名相似或含义相同，例如"演出时间 / 申请日期 / 场次"，是否确认实际来源一致，而不是一个取订单顶层字段、一个取 skuDTOList/specsList？
- 有兜底字段时，是否确认优先级不会导致目标字段永远不生效？例如 `A || B` 中 A 常年有值时，B 的修改不会生效。

空值/边界：
- 列表为空时是否有空状态 UI？
- 详情页字段缺失时是否有默认值保护？
```

---

## 高风险版（涉及时间 / 分类扩展 / 排行榜时用这个）

```
你刚才写完了代码，这次改动属于高风险类型，需要重点 review。

【本次改动】
- 改动模块：___
- 改动内容：___
- 高风险类型（勾选）：
  □ 涉及活动时间/抢票时间
  □ 涉及新增分类或修改分类逻辑
  □ 涉及排行榜/排名计算
  □ 涉及分页逻辑

【要求】
针对勾选的高风险类型，逐项详细说明你的处理方式。
如果某项存在风险或你不确定，明确说出来，不要跳过。

【时间相关检查】（勾选了时间类必查）
- 时间限制是否在服务端校验？前端只是体验层，不是安全层。
- 用户直接访问活动路由（绕过正常入口）时，页面是否主动拉取并校验活动状态？
- 倒计时在页面切回前台后是否会重新校准？
- 活动结束/未开始时，提交接口是否会被服务端拦截？

【分类扩展检查】（勾选了分类类必查）
- 新分类的特殊逻辑（上传限制、提示文案、表单字段）是否用分类 ID/类型做了条件判断？
- 有没有把新分类的规则写进公共逻辑？如果有，其他分类会受什么影响？
- 旧分类的行为是否完全未变？请列出你验证过的旧分类。
- 仅拍照的分类，sourceType 是否只传了 ['camera']，相册入口是否屏蔽？

【排行榜/排名检查】（勾选了排行榜类必查）
- 排名是基于全量数据计算，还是当前页数据？
- 前三名的特殊样式，是判断全局排名字段，还是判断当前页的位置索引？
- 第二页的第一条数据，排名数字是多少？（必须是接续第一页的，不能重新从 1 开始）
- 分页参数 offset/page 是否正确传给了接口？

【分页逻辑检查】（勾选了分页类必查）
- 下拉刷新后页码是否重置为第一页？
- 最后一页"加载更多"按钮是否正确禁用？
- 总数展示是否与实际列表条数一致？
```

---

## 使用说明

1. **选版本**：日常改动用普通版，涉及时间/分类/排行榜用高风险版
2. **填写改动信息**：把"___"和勾选项填完再发给 AI
3. **看 AI 的回答**：回答里出现"不确定"或"可能影响"的地方，自己手动验一遍
4. **AI 说没问题不代表没问题**：时间边界、多分类点击验证这类，必须自己跑

---

## 发给 AI 之前，先自问三个问题

- 需求里有没有未确认的时间或配置？（有的话先确认，不要先写代码）
- AI 改动有没有动公共逻辑？（有的话高风险版必用）
- 这个功能之前出过 bug 吗？（出过的地方重点验）
