# AI 代码 Review Prompt

---

```
你刚才写完了代码，现在按以下三步做自我 review。

---

## 第一步：根据 git diff 匹配涉及模块

分析本次所有改动文件，自动匹配以下模块，可同时命中多个。
**按改动类型判断，不按文件名判断。**
例如：改了排行榜逻辑 → 同时命中「数据分页」「缓存」「并发与竞态」。

| 改动涉及 | 命中模块 |
|----------|----------|
| 业务字段、变量名、数据来源 | 数据一致性 |
| 时间展示、倒计时、活动状态 | 时间状态 |
| 列表分页、排名、榜单 | 数据分页 |
| Storage、缓存、本地存储 | 缓存 |
| 搜索、筛选、Tab 切换、并发请求 | 并发与竞态 |
| onShow/onLoad/跳转/页面栈/EventChannel/事件总线/globalData | 生命周期 |
| 新增分类/类型、分类条件判断 | 公共组件 |
| 公共组件、公共函数 | 公共组件 |
| 权限、接口鉴权、入口控制 | 权限校验 |
| 表单、提交按钮、上传 | 表单提交 |
| WebSocket、长连接 | 长连接 |

列出本次命中的模块，然后执行第二步。

---

## 第二步：执行命中模块的检查

逐项回答，不能只说"已检查"，要说明怎么处理的或为什么不涉及。
有风险直接说，不要美化。

---

### 数据一致性

- 同一业务含义在列表页、详情页、卡片组件、提交/回显页是否取自同一数据源？
- 修改了业务字段、变量名或判断逻辑，是否全局搜索了所有使用该字段/同义概念的位置？请列出清单并逐一确认是否需要同步修改。
- 字段名相似但来源不同（如订单顶层字段 vs skuDTOList vs specsList），是否确认实际取值层级一致？
- 有 `A || B` 兜底时，是否确认 A 不会常年有值导致 B 的修改永远不生效？
- 有 `||` 或 `??` 兜底的地方，是否确认了原始字段取值本身正确？不能只看最终展示结果正常，兜底值会掩盖字段一直没取对的问题。

---

### 时间状态

- 时间限制是否在服务端校验？前端只是体验层，不是安全层。
- 用户直接访问活动路由（绕过正常入口）时，页面是否主动拉取并校验活动状态？
- 倒计时在页面切回前台后是否重新校准？
- 活动结束/未开始时，提交接口是否会被服务端拦截？
- 时间字符串（非时间戳）在 iOS 上是否测过？`new Date('2026-01-01 10:00:00')` 这种"横杠+空格"格式在 iOS 上解析为 Invalid Date，需替换为 `2026/01/01 10:00:00` 或直接用时间戳（不要用不带时区后缀的 `T` 格式——旧版 Safari 按本地时间解析、新版按 UTC 解析，二者会差 8 小时，比 Invalid Date 更隐蔽）。

---

### 数据分页

- 排名是基于全量数据计算，还是当前页数据？
- 前三名的特殊样式，是判断全局排名字段，还是当前页的位置索引？
- 第二页第一条的排名数字是否接续第一页，不能重新从 1 开始？
- 分页参数 offset/page 是否正确传给了接口？
- 下拉刷新后页码是否重置为第一页？
- 最后一页"加载更多"是否正确禁用？
- 总数展示是否与实际列表条数一致？
- 快速切换筛选/Tab 并发触发时，是否取消了上一次未完成请求，或校验了响应与当前状态一致？

---

### 缓存

- 这份缓存由哪个页面在什么时机写入？分享直达、扫码直达、清缓存后首次进入时，缓存是否为空？为空时页面表现是什么？
- 缓存缺失时是重新请求接口/使用合理默认值，还是直接显示空数据或走错分支？
- 缓存 key 是否统一维护在一处，禁止多处硬编码拼接？
- 该页面是否依赖其它页面先打开、先写入状态才能正常工作？
- 接口返回新数据后，缓存与页面状态是否同步更新？
- 切换账号后是否会重新拉取数据，不能继续使用旧用户遗留的缓存？

---

### 并发与竞态

- 搜索框/筛选条件/Tab 切换时，旧请求比新请求晚返回是否会覆盖正确结果？是否有防抖或取消上一次请求？
- 页面未 ready 时用户已触发提交，参数是否为空或初始值？
- `Promise.all` 里单个接口失败时，其他成功的数据是否有降级处理，而不是全部丢弃？
- token 过期时多个并发请求是否会重复触发刷新？
- 接口报错时 loading 是否关闭，不能永久 loading？
- 页面切换/组件卸载后，未完成的请求返回时是否还会执行 setData（可能导致卸载后报错，或数据写到了错误的页面实例上）？

---

### 生命周期

- 页面是否依赖 onLoad 刷新数据？通过 tabBar 切换或返回上一页进入（只触发 onShow）时，数据是否仍能正确刷新？
- onShow 会被切后台回前台、返回上一页、授权弹窗关闭等多种场景触发，onShow 里的请求/事件注册是否会重复执行、重复注册？
- 新增的 timer、事件监听，是否在 onHide/onUnload 时清理？
- 页面栈是否可能超过 10 层？navigateTo 超限后静默失败，是否有降级到 redirectTo？
- 跳转目标是否可能是当前页面本身，导致栈里堆叠相同页面？
- 使用了 EventChannel 时，页面是否可能被 redirectTo/分享/扫码打开而拿不到 EventChannel？
- 使用了 getCurrentPages() 反向操作上一页时，页面栈结构变化（直达、redirectTo）是否会导致出错？

---

### 公共组件

- 修改了公共组件/公共函数，是否验证了所有调用方的表现，不只是当前需求的入口？
- 某个分类/类型的特殊逻辑，是否用条件判断隔离，而不是直接改进公共逻辑？
- 旧分类/旧逻辑的行为是否验证未受影响？请列出已实际点击验证过的对象，不能只看代码。
- 条件渲染是否正确区分 `wx:if` 和 `hidden`？用 `hidden` 隐藏的组件仍会初始化并触发接口请求，权限/付费内容不能用 `hidden`。
- 父组件 props 更新后，子组件内部是否有复制的 data 副本未同步？
- `wx:key` 设置是否正确？设错会导致列表切换时复用了旧实例，内部状态残留。

---

### 权限校验

- 新增接口是否有权限校验？
- 新增页面是否校验了登录态？未登录时应跳转登录，而不是白屏或报错。
- 前端隐藏入口的同时，后端是否也做了拦截？不能只靠前端路由拦截。
- 越权访问：修改请求参数中的 ID 是否能访问到他人数据？

---

### 表单提交

- 重复提交是否有防护？提交后按钮是否禁用？
- 提交失败后按钮是否恢复可点击？
- 必填项为空提交是否有明确错误提示？
- 仅拍照的分类，sourceType 是否只传了 `['camera']`，相册入口是否屏蔽？
- 上传中断或失败后，是否退出 loading 状态并支持重试，不能卡住？
- 金额/数字是否用浮点直接计算？`0.1 + 0.2` 有精度问题，金额计算需用整数或专用库。
- 后端返回大整数（订单号、用户 ID > 2^53）是否用字符串接收？JSON 解析会丢精度。

---

### 长连接

- 小程序切到后台再回到前台后，连接是否重新建立，而不是假设一直存活？
- 重连成功后是否补偿性拉取一次数据？
- 消息是否可能重复或乱序？是否做了去重/排序？
- 重连策略是否有退避（指数退避/最大重试次数），避免断线后无限高频重连？
- 页面卸载时是否主动关闭连接？

---

## 第三步：最终全局检查（必须执行，不可跳过）

与模块无关，所有改动都要过这一遍：

- 修改了 A，是否遗漏了 B/C/D 同类逻辑（同一业务概念在多处各自实现）？
- diff 中被删除的代码行，是否确认是本次需求要求删除的，而不是顺手误删（尤其是别的分类/场景的特殊判断逻辑）？
- 只判断了 HTTP status 200，是否同时判断了业务 code？
- 新旧字段是否混用？
- 新旧接口是否混用？
- 新增枚举/类型是否覆盖了全部 switch/if 分支？
- 修改了一个入口，是否遗漏了其它入口？
- 新增配置是否同步到前端/后端/数据库？
- 注释是否与实际逻辑一致？改了逻辑是否同步更新了注释？
- 条件判断是否只写了 if，遗漏了 else 分支？
- 有没有遗漏的状态判断（如已取消、已关闭）？
- 状态切换后，页面上所有相关 UI（按钮、标签、提示）是否同步更新，不能停留在旧状态？
- 是否有遗漏的空值/边界保护（列表为空、字段缺失、接口异常）？
```

---

## 使用说明

1. **把改动的 git diff 和代码一起发给 AI**：第一步由 AI 自动匹配模块，不用人工先判断"这次算不算高风险"
2. **看 AI 匹配的模块是否合理**：如果 AI 漏判了某个明显相关的模块（比如改了缓存却没匹配上「缓存」），直接指出来让它补查
3. **看 AI 的回答**：回答里出现"不确定"或"可能影响"的地方，自己手动验一遍
4. **第三步的全局检查不能省**：这一步和具体命中的模块无关，任何改动都要过一遍
5. **AI 说没问题不代表没问题**：时间边界、并发竞态、分享/扫码直达这类场景，必须自己跑一遍

---

## 发给 AI 之前，先自问三个问题

- 需求里有没有未确认的时间或配置？（有的话先确认，不要先写代码）
- AI 改动有没有动公共逻辑？（有的话尤其要看「公共组件」模块的检查结果）
- 这个功能之前出过 bug 吗？（出过的地方重点验）
