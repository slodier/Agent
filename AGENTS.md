# GOLANG
## GORM
- 禁止在 for 循环中执行sql
- MySQL8 中优先使用递归
- GORM 事务优先使用：
  err := pkg.Db.Transaction(func(tx *gorm.DB) error {
- 尽量避免手动 tx.Begin()
- 如必须使用手动事务，必须保证所有分支都有 Commit 或 Rollback
- 禁止事务中包含慢接口、远程请求、文件上传等耗时操作
- 事务内只放必须保证一致性的数据库操作
- 错误事务处理可能导致锁长期不释放，引发大量锁等待
- 高并发场景必须控制事务范围，避免长事务扩大锁影响

# 数据库
- 严格使用真实字段名
- 不允许猜字段
- 不允许自动生成不存在字段
- SQL 必须确认：
    - LEFT JOIN 是否导致重复数据
    - OR 条件是否导致索引失效
    - 实时写入是否导致结果不一致
- 涉及并发数据时，优先使用行锁，避免表锁
- 必须确认 where 条件命中索引，否则可能退化为大量锁记录
- 锁范围必须最小化
- 长事务必须确认是否阻塞其它业务
- 必须确认是否可能产生死锁  

# Vue3
## 日期时间规则

- 涉及“日期/天数/签到/排行/今日/昨日”等业务时，必须先确认：
    - 使用 UTC
    - 用户本地时区
    - Asia/Shanghai（北京时间）
    - 或服务端固定时区
- 时区未明确前，不允许擅自实现日期逻辑
- 禁止使用 new Date('YYYY-MM-DD') 解析业务日期
- `YYYY-MM-DD` 会按 UTC 0 点解析，东八区会变成当天 08:00
- 按北京时间/业务日期计算时，必须固定 Asia/Shanghai
- 起始日必须用：
    - 服务端返回时间戳；或
    - new Date(year, month - 1, day)；或
    - 服务端返回 YYYY-MM-DD 后，必须拆分成年月日再构造 Date
- 禁止直接解析 YYYY-MM-DD 字符串
- 禁止把“服务端时间戳”和“前端本地时区日期”混算
- 时间戳必须确认是秒还是毫秒
- 计算天数前，必须确认是：
    - UTC 日期
    - 用户本地日期
    - 北京时间业务日期

## 风格
- setup 语法
- 使用箭头函数
- 方法和数据按功能放一起
- 不要 options api
- composable 按功能拆分
- UI 块较复杂/重复时，适当拆分子组件
- 超过一个文件复用的方法，抽通用方法
- 方法过长时，必须拆分/解耦
- 禁止超大组件
- 禁止单文件几千行
- 类型必须明确
- props/emits 必须定义
- watch 前先确认 computed 是否更适合
- 禁止滥用 watch

## 请求
- 登录失效：
  return new Promise(() => {})
- 不允许 Promise.reject('')
- axios 必须统一封装
- 请求必须处理 loading
- 请求必须加锁，防止重复请求
- 不仅表单提交，列表/按钮/初始化请求也要防重复触发
- 表单提交必须防抖/锁定
- 删除操作必须二次确认

## 样式
- 不允许 inline style
- 使用 class
- SCSS 嵌套
- class 名可读
- uniapp 单位 rpx; 其它 px
- 不允许 !important 泛滥
- 不允许深层级嵌套
- 不允许复制大量重复 css
- 优先组件化样式
- 如果是从蓝湖/墨刀复制的 html/css，需要去掉无用结构和无用样式
- UI 还原后，必须优化 html 结构和 css 层级
- 禁止保留大量无意义 div 嵌套
- 相同样式必须合并，避免重复 css
- HBuilder/uniapp 中禁止直接使用 tailwind.css
- 项目未接入 tailwind.css 时，禁止直接使用 tailwind class
- 页面颜色保持统一
- 暗色/亮色状态必须确认

## 排行/排名规则
- 排名分页时必须按全局偏移计算

# UniApp / 小程序
- web-view 跳小程序：
  wx.miniProgram.navigateTo
- 图片长按识别：
  show-menu-by-longpress
- web-view 中 H5 页面长按二维码识别，仅支持：
  - 小程序码
  - 微信个人码
  - 企业微信个人码
  - 普通群码
  - 互通群码
  - 公众号二维码
- 其它二维码不保证支持识别
- 小程序内 H5（web-view）长按识别二维码，属于微信能力限制
- 非微信体系二维码可能无法识别
- chooseImage/chooseVideo 返回会触发 onShow
- uni.showToast 会被系统清掉
- 代码确认无误但多次修改不生效时，尝试关闭 HBuilder 后重新运行微信小程序
- 不允许频繁 onLoad 请求
- 长列表必须分页
- 图片必须懒加载
- 视频必须确认码率
- 必须确认安卓/iOS 差异
- 必须确认微信/支付宝差异
