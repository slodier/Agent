# 上线前检查（小程序 + 移动端 H5）

> AI 生成代码后必须逐项过一遍，不能只看改动点，要验证关联影响。

---

## 零、上线前确认（最高优先级，代码都不用看，先过这关）

> ⚠️ 这一类问题 AI 无法检查，也无法通过 Review 发现，根本原因是**需求信息不完整**就开始写代码。错的时间还能改，没确认的时间根本不知道该写什么。

**时间 / 配置类**
- 活动开始时间是否已由需求方明确确认（不能自己估一个）
- 活动结束时间是否已确认，且是最终版本（警惕中途改时间没同步到你）
- 按钮显示/隐藏的触发时间是否已确认，数据库里写的值是否和确认结果一致
- 所有写死在数据库/配置里的时间字段，上线前逐一核对一遍
- 如有时间改动，是否通知了所有相关方（后端/运营/测试）

**需求变更类**
- 本次上线前，是否有需求在开发过程中被口头修改过但未正式记录
- 群消息/口头沟通中的改动，是否已同步到代码和配置
- 有没有"以为对方改了，实际没改"的功能点（跨项目、跨端尤其要问清楚）

**发布顺序与版本兼容**
- 前端/后端/配置的上线顺序是否已确认，中间态（一方已上线、一方未上线）下功能是否仍可用
- 小程序发版有审核期，且旧版本会在用户手里存留数天到数周；本次接口改动是否向后兼容旧版本小程序（不能直接删字段、改枚举含义），必须能兼容旧版本客户端，而不是指望用户很快更新

**未确认项卡控**
- 有任何未确认的配置/时间/规则时，**不允许上线**，必须先对齐

---

## 一、时间相关

- 活动开始/结束时间是否使用服务端时间，而非客户端本地时间
- 倒计时是否在页面切换后/回到前台后重新校准，而非继续原来的计时器
- 跨天场景：0 点前后状态切换是否正确（如"今天"变"昨天"、活动结束）
- 时间格式：接口返回的时间优先使用 Unix 时间戳（没有时区歧义）；如果是字符串（如 `2026-01-01 10:00:00`），前后端是否明确约定了时区，不能各自默认对方按本地时间/UTC 处理
- iOS 兼容：`new Date('2026-01-01 10:00:00')` 这种"横杠+空格"格式在 iOS Safari / 微信 WKWebView 下会被解析为 `Invalid Date`，需要转换成 `2026/01/01 10:00:00` 或直接用时间戳；此问题在 Android、开发工具、模拟器下完全正常，只有真机 iOS 才会暴露，非常容易漏测
- 抢票/报名时间：是否有服务端二次校验，不能只靠前端禁用按钮拦截
- 直接访问活动路由（绕过入口）：页面初始化时是否主动拉取并校验活动状态，而非依赖上一页传参
- 时间限制的校验逻辑在**服务端**，前端路由拦截只是体验层，不是安全层

---

## 二、排行榜

> ⚠️ 真实 Bug：排行榜无分页时，每页数据独立计算排名，导致每页都出现第 1/2/3 名

- 排名是否基于**全量数据**计算，而非当前页数据
- 第二页第一条的排名是否接续第一页（不能重新从 1 开始）
- 前三名的特殊样式（金银铜牌）只能在**全局排名 1/2/3** 时出现，不随分页重置
- 分页参数 offset/page 是否正确传给接口，是否验证过第二页数据不重复
- 数据为空时：空状态 UI 是否正常，不能显示空列表或报错
- 数据量很大时：是否有分页或截断保护，避免一次性渲染卡顿

---

## 三、分类 / 类型扩展

> ⚠️ 真实 Bug：新增分类时 AI 将新分类的特殊逻辑（仅拍照、特殊表单提示）改进了公共逻辑，导致所有分类行为改变

- 新分类的特殊规则（上传限制、表单提示、审核流程）是否通过**分类 ID / 类型字段**做条件判断，而非直接修改公共逻辑
- 修改某一分类的逻辑后，其他分类的上传、表单、提示文案是否**未受影响**（必须实际点进去验证）
- 公共组件/公共函数是否被新增了只对特定分类生效的参数——这是危险信号，应优先用策略模式或配置隔离
- 新分类的限制（如仅允许拍照、不允许从相册选取）只在该分类下生效，旧分类行为不变
- 新分类的特殊提示文案在该分类下显示，旧分类仍显示原来的提示文案

---

## 四、权限

- 未登录用户：访问需登录页面是否跳转登录，而非白屏或报错
- 普通用户：是否能访问到管理员才能看的入口或接口（抓包验证，不能只看 UI 隐藏）
- 管理员：操作权限是否在服务端校验，不能只靠前端路由拦截
- 越权访问：修改请求参数中的 ID 是否能访问到他人数据（必须测试）
- 特殊角色（如审核员、运营）：权限边界是否正确，既不过宽也不过窄

---

## 五、活动状态

- 活动未开始：入口是否隐藏或禁用，点击是否有明确提示
- 活动进行中：所有功能正常，状态展示正确
- 活动已结束：入口是否隐藏，已参与数据是否仍可查看，不能直接 404
- 活动状态切换后：页面是否自动刷新或响应变化，不能停留在旧状态
- 活动状态由后端控制，前端不能绕过状态直接提交

---

## 六、分页

- 第一页数据是否正确，没有偏移错误
- 第二页是否有数据，且与第一页不重复
- 最后一页：没有"加载更多"按钮，或按钮状态正确禁用
- 总数展示是否与实际数据一致
- 空数据：空状态 UI 是否正常，不能显示"第 0 条/共 0 条"这类奇怪文案
- 下拉刷新后：页码是否重置为第一页

---

## 七、请求竞态与异步处理

> ⚠️ 常见坑：快速切换筛选条件/Tab/分类时，先发出去的慢请求比后发出的快请求晚返回，覆盖了最新状态，页面出现"新 Tab 的标题 + 旧 Tab 的列表"这种错配现象。开发时手动点击间隔较长，很难复现，用户快速连续点击时才会暴露。

**请求竞态**
- 切换筛选条件/Tab/分类时，是否会取消上一次未完成的请求（`AbortController`、取消 token、请求序号校验），而不是放任其自然返回后覆盖数据
- 响应到达时是否校验了"发起请求时的参数"与"当前页面状态"是否一致，不一致则直接丢弃该次响应
- 下拉刷新与上拉加载更多是否可能并发触发，是否有加锁/防抖，避免两次响应交错导致列表错位或重复数据
- 连续输入触发的搜索接口（如即时搜索框）是否做了防抖/节流，并同样处理响应乱序的问题
- 页面切换/组件卸载后，未完成的请求返回时是否还会执行 `setState`/`setData`（可能导致卸载后报错，或数据写到了错误的页面实例上）

**异步处理**
- 页面/组件尚未初始化完成（数据未 ready）时，用户已经触发提交或操作，此时拿到的参数是否是空值或默认初始值，而不是用户实际选择的值
- `Promise.all` 中某一个接口失败时，其它已成功的数据是否直接被整体丢弃；是否应该改成允许部分失败、做降级展示，而不是一个接口挂了整页跟着白屏
- token 过期刷新逻辑：多个并发请求同时触发刷新时，是否会重复刷新多次（应做去重/排队，只让第一个请求触发刷新，其余请求等待并复用结果）
- 接口报错或超时后，页面 loading 状态是否被正确关闭，不能停留在永久 loading；多个并发接口共用一个 loading 状态时，是否会因为其中一个提前完成而误关闭其它接口还在用的 loading

---

## 八、表单

- 必填项：为空提交是否有明确错误提示，提示定位到具体字段
- 选填项：为空提交后接口是否正常处理（不能传 null 导致后端报错）
- 默认值：是否在打开表单时正确填充，而非每次都是空
- 重复提交：按钮是否在提交后禁用，防止重复请求
- 提交成功后：是否有成功提示，页面/列表是否刷新
- 提交失败后：错误信息是否展示，按钮是否恢复可点击

---

## 九、上传（小程序重点）

- 图片上传：是否区分拍照和从相册选取，权限申请是否处理（用户拒绝后的降级）
- 授权拒绝后引导：用户拒绝相机/相册/麦克风授权后，再次调用不会弹出授权弹窗，是否引导用户跳转 `wx.openSetting` 手动开启，而不是静默失败或反复无响应
- 视频上传：是否有时长限制、大小限制，超出时是否有提示
- 数量限制：达到上限后上传入口是否禁用，不能继续触发选择
- 格式限制：不支持的格式是否有明确提示，而非静默失败
- 删除后重新上传：旧文件是否正确清除，不能新旧混存
- 上传中断/失败：是否有重试机制或错误提示，不能卡在 loading 状态
- **新分类特殊限制**：仅拍照的分类，相册入口是否完全屏蔽（小程序 `sourceType` 只传 `['camera']`）

---

## 十、状态流转

- 各状态之间的流转路径是否与产品设计一致（如草稿→审核中→已发布）
- 不合法的状态跳转是否被拦截（如已取消的订单不能再支付）
- 状态切换后，页面上所有相关 UI（按钮、标签、提示）是否同步更新
- 状态由后端返回，前端不能本地缓存旧状态导致展示错误

---

## 十一、隐式依赖 / 缓存

> ⚠️ 真实高风险点：开发时按固定流程进入页面没问题，但用户首次进入、清缓存、扫码直达、分享直达或从别的入口进入时，依赖的缓存根本不存在。

**缓存读写**
- 所有读取 Storage / LocalStorage / Cache 的代码，都要确认缓存是谁写入的
- 缓存读写是否成对出现，不能只有读取逻辑却找不到可靠写入入口
- 缓存 key 是否统一维护，禁止多个页面手写相同 key 导致拼写不一致
- 缓存数据结构是否有版本兼容，旧版本缓存字段缺失时不能白屏或走错逻辑
- 缓存过期、清理、切换账号后是否会重新拉取数据，不能继续使用旧用户数据

**页面独立性**
- 禁止页面依赖其它页面初始化数据；每个页面都应具备独立工作的能力
- A 页面生成缓存，B 页面直接读取缓存，属于隐藏依赖，必须重点标记
- 分享进入、扫码进入、刷新进入、从消息/订阅通知进入时，页面是否能独立初始化
- 返回上一页后依赖上一页残留状态的逻辑要重点检查，不能假设页面栈一定存在

**缓存兜底**
- 有缓存可以使用缓存；无缓存时，必须重新请求接口、重新初始化，或提供合理默认值
- 禁止因为缓存不存在直接返回空数据，除非业务明确允许空状态
- 读取缓存后必须处理空值、格式异常、字段缺失、JSON 解析失败等情况
- 接口返回新数据后，缓存与页面状态是否同步更新，不能页面显示新数据但缓存仍是旧数据

**环境差异**
- 是否存在"默认缓存一定存在"的假设，特别是首次安装、首次进入、清缓存后的场景
- 是否存在依赖其它页面打开后才能正常工作的逻辑
- 是否存在开发环境正常、正式环境异常的问题，如缓存 key、配置值、环境变量、接口 baseURL 不一致
- 是否存在开发工具里因为历史缓存正常，真机新用户却异常的情况
- 请求/上传域名是否已在小程序后台配置合法域名；开发工具"不校验合法域名"选项打开时一切正常，真机因域名未加白名单会直接失败，且失败原因经常被误判为业务 Bug 而排查很久

---

## 十二、小程序生命周期

> ⚠️ 常见坑：开发时按固定路径（首页进入详情页）测试一切正常，但 tabBar 切换、返回上一页、切后台再回前台等场景不会重新触发 `onLoad`，只会触发 `onShow`；遗漏 `onShow` 里的刷新逻辑，会导致数据陈旧却不容易被发现。

**onLoad / onShow**
- 从详情页操作（提交、取消、点赞等）返回列表页后，列表页是否在 `onShow` 里刷新，而不是只在 `onLoad` 里请求一次
- tabBar 页面切换只触发 `onShow`，不会重新触发 `onLoad`；依赖 `onLoad` 传参或初始化的逻辑在 tabBar 页面下是否仍然生效
- `onShow` 会被切后台回前台、返回上一页、授权弹窗关闭等多种场景触发，`onShow` 里的请求/事件注册是否会重复执行、重复注册

**页面销毁与资源清理**
- 倒计时等 timer 是否在 `onHide`/`onUnload` 中清除；不清除会导致返回再进入同一页面后出现多个并行计时器（倒计时一秒跳多次）
- 页面内注册的事件监听、订阅是否在 `onUnload` 中注销（详见十四、跨页面通信）

**冷启动竞态**
- `App.onLaunch` 中的异步登录（code2session 换 token）与首页 `onLoad` 是否存在竞态：登录未完成时页面已发出需要 token 的业务请求，导致请求带空 token 失败；扫码直达、分享直达等冷启动场景下最容易复现，开发时保持登录态热启动无法发现这个问题

**组件复用与状态残留**
- 父组件 `properties` 更新后，如果子组件内部把 props 复制进自己的 `data`（如 `data: { localValue: this.properties.value }`），后续 props 变化时这份内部拷贝是否会同步更新，还是只在组件初始化时复制了一次
- `wx:if` 切换导致组件销毁重建时，`wx:key` 是否设置正确（不能用数组 index 当 key）；key 设错会导致节点复用了错误的组件实例，内部状态没有被清空
- 列表/Tab 复用场景（如 swiper 多个 tab 共用同一套组件），切换 tab 后，上一个 tab 的滚动位置、loading 状态、选中状态是否残留到了新 tab

**页面栈与跳转**
- 连续快速点击入口是否会触发多次 `navigateTo`，开出多个重复页面；是否需要防抖，或改用 `redirectTo`
- 页面栈层级是否可能超过小程序的跳转层数上限（如 A→B→A→B 循环跳转场景）；接近上限时应改用 `redirectTo`/`reLaunch`，而不是一路 `navigateTo`
- 跳转逻辑是否处理了"当前页面就是目标页面"的自跳转场景；未判断时会不断 `navigateTo` 堆出多个相同页面，间接加速触碰上面的层级上限

---

## 十三、WebSocket / 长连接

> ⚠️ 常见坑：开发联调时网络稳定，socket 几乎不断线，隐藏了断线重连、消息补偿等逻辑的缺失，只有到真实弱网、长时间使用、频繁切后台的场景才会暴露。

- 小程序切到后台约 5 秒后连接会被系统挂起甚至断开，`onShow` 时是否检测连接状态，需要时主动重连
- 重连成功后是否会拉取一次增量或全量数据做补偿，不能假设断线期间收到的消息会自动补发
- 是否处理了消息重复（重连后收到已处理过的消息）和乱序（到达顺序与发送顺序不一致）的问题，是否按消息 ID/序号做了去重和排序
- 是否有心跳机制识别"连接对象仍在但实际已不可用"的假死状态（TCP 半开连接），并在超时后主动重建连接
- 重连策略是否有退避（指数退避/最大重试次数），避免断线后无限高频重连
- 页面卸载时是否主动关闭连接，避免多个页面各自建连导致连接数超出小程序 `connectSocket` 的并发上限

---

## 十四、跨页面通信

> ⚠️ 常见坑：EventChannel、事件总线这类跨页面通信方式都隐式假设了"页面是按预期路径打开的"，一旦用户从分享链接、扫码、消息通知等非常规入口进入，通信链路会直接失效或取不到数据，且这类问题很难在正常开发路径下复现。

**EventChannel**
- EventChannel 只在使用 `navigateTo` 打开页面时有效；页面通过 `redirectTo`、`reLaunch`，或由分享/扫码直达打开时，`getOpenerEventChannel()` 拿不到有效 channel，代码是否有兜底，而不是直接报错或静默拿到 `undefined`

**事件总线 / 全局事件**
- 页面内通过事件总线注册的监听（`on`）是否在页面卸载时对应注销（`off`）；不注销会导致页面反复进出后同一事件触发多次（重复弹窗、重复请求），残留的回调还可能引用已销毁的页面实例而报错
- on/off 是否成对出现——可以类比十一章"缓存读写必须成对出现"的检查方式

**全局状态**
- 直接修改 `globalData` 或全局单例状态后，其他页面是否只有重新进入 `onShow` 主动读取才能感知变化（`globalData` 本身不是响应式的），依赖它自动刷新 UI 的写法是否会失效
- 是否存在通过 `getCurrentPages()` 反向获取并修改上一页数据的写法；这类代码隐式假设了固定的页面栈结构，一旦入口变为直达/`redirectTo`，页面栈里找不到预期页面就会出错，应视为危险信号，优先改用回调、EventChannel 或状态管理

---

## 十五、小程序 / H5 特有

**通用**
- 分享卡片：标题、图片、路径参数是否正确，带参数落地页是否正常
- 底部安全区：按钮/Tab Bar 是否适配 iPhone 底部横条（`safe-area-inset-bottom`）
- 键盘弹起：输入框是否被键盘遮挡，页面是否正确上推；键盘收起后，`fixed` 定位元素是否会错位或底部留白，是否需要在 `blur` 时手动 `scrollTo` 兜底
- 弹窗/遮罩：打开后底层页面是否还能滚动（滚动穿透）；关闭弹窗后页面滚动位置是否丢失
- 长列表性能：超过 50 条数据是否有虚拟列表或分页，避免卡顿白屏
- 网络异常：断网/弱网下是否有提示，关键操作是否有重试

**小程序补充**
- 条件渲染是否正确区分 `wx:if` 和 `hidden`：用 `hidden` 隐藏的组件仍会初始化并可能触发内部的接口请求/生命周期，权限判断、付费内容、敏感信息这类不能只用 `hidden` 隐藏，必须用 `wx:if` 保证组件真正没有被创建
- `rpx` 与 `px` 混用：同一页面/组件内两种单位混用，会导致不同设备下的换算基准不一致，实际显示比例跑偏，尤其是从 H5 代码迁移或多端复用组件时容易漏改
- `setData` 性能：单次 `setData` 是否传了超大数据（整个列表、大对象）；高频场景（倒计时每秒更新、滚动监听）是否只 set 变化的字段路径，而不是整个对象
- 小程序码/二维码的 `scene` 参数最长 32 字符且字符集受限，参数较多的落地页是否改为短码换取完整参数，而不是直接拼进 scene
- 支付结果确认：`wx.requestPayment` 的 `success` 回调不可靠（用户支付成功后杀进程、网络问题都可能导致回调丢失），页面 `onShow` 时是否会向服务端查单确认支付状态，而不是只依赖前端回调更新订单状态

**H5（微信内置浏览器）补充**
- JSSDK 签名：SPA 路由切换后，iOS 用首次进入页面的 URL 参与签名、Android 用当前 URL，两端签名 URL 取值不一致导致的分享/支付调用失败是否都测过
- 微信 JS-SDK `config` 签名失败时通常是静默的（不会白屏报错），分享、录音等功能界面看起来正常，实际调用时才会发现没有权限；是否监听了 `wx.error` 回调并有可观测的日志或提示，而不是等用户反馈才发现
- 微信 OAuth：授权回调的 `code` 只能消费一次，用户在回调落地页刷新会带着已消费的 `code` 重新换 token 而失败，是否在换取成功后清空 URL 上的 `code`（`history.replaceState`）
- 缓存：微信内置浏览器缓存策略激进，发版后用户容易命中旧的 HTML/JS；静态资源是否带 hash，HTML 是否设置 `no-cache`
- 分享链接：微信会在分享出去的链接后追加 `from=singlemessage` 等参数，路由匹配、签名 URL 拼接逻辑是否能容忍这些多余的 query 参数
- 音视频自动播放：`audio`/`video` 元素是否只在用户手势触发或 `WeixinJSBridgeReady` 事件后才尝试播放，而非页面加载后直接 `play()`
- `history.back()` 在微信内置浏览器里行为不稳定，有时会直接退出 webview 而不是返回上一页；是否维护了自己的路由栈来判断该调用 `history.back()`、`wx.miniProgram.navigateBack` 还是直接关闭页面
- iOS Safari 下 `100vh` 会把地址栏/工具栏高度也算进去，导致页面底部内容被遮挡；是否用了 `-webkit-fill-available` 或 JS 计算实际视口高度做兼容
- iOS Safari 和 Android 微信内置浏览器是否都测过（不只是微信内打开，独立 iOS Safari 打开的兼容性也要覆盖）

---

## 十六、数据展示

- 列表页：空数据、单条数据、大量数据三种场景
- 详情页：所有字段是否有空值保护（字段缺失不能白屏或显示 undefined）
- 同一业务字段在列表页、详情页、卡片组件中的展示来源是否一致；不能只看字段名，要确认接口层级一致，例如订单顶层字段、skuDTOList、specsList、productListDTO
- 数字/金额：小数位、千分位格式是否正确
- 金额计算：涉及金额加减（如满减、合计、优惠计算）是否用分为最小单位或做了精度处理，避免直接对浮点数做加减导致的精度误差（如 `0.1 + 0.2 !== 0.3`）
- 大整数精度：后端返回的大整数字段（订单号、用户 ID 等超过 2^53 的数值）如果按 JSON Number 返回，前端 JSON 解析后可能丢失精度；这类字段后端应返回字符串，前端接收和展示时也要按字符串处理，不能转成 Number 再拼接
- 长文本：是否有截断处理，展开/收起是否正常
- 统计数字：汇总数据是否与列表数据吻合，不能对不上

---

## 十七、回归检查

- 本次改动的功能：核心路径全部走一遍
- 关联页面：入口页、上下游页面是否正常
- 关联接口：改动涉及的接口，其他调用方是否受影响
- 旧分类 / 旧功能：确认行为未变化（不能只看代码，要实际点击验证）
- **问题闭环确认**：之前发现的 bug 或讨论过的修改，是否亲自确认已上线生效——不能依赖"对方说改了"，跨项目/跨端的改动尤其要自己验证

---

## 十八、人工抽查 AI 自查结果（复核 REVIEW_PROMPT 的输出，而非重新跑一遍）

> 这一章不是让你把 REVIEW_PROMPT.md 的检查再跑一遍，而是拿着 AI 已经给出的自查回答，重点核对下面这几类问题有没有被敷衍带过。AI 说"已检查""无影响"这类话本身不算数，要看它有没有说清楚具体是怎么处理的。

**改公共逻辑前必问：**
- 这个改动是否只对某一分类/类型生效？如果是，必须加条件判断，不能直接改公共代码
- 修改了公共组件/公共函数后，是否验证了**所有调用方**的表现，而非只测当前需求的入口

**业务字段/逻辑一致性：**
- 每次修改业务字段、变量名或判断逻辑，必须全局搜索项目里所有使用该字段/变量名/同义业务概念的地方，并列出清单逐一确认是否需要同步修改
- 发现多个页面/组件各自实现同一业务概念时，必须标记为高风险；能抽公共 getter、工具函数或配置时，优先收敛到统一入口

**排名/分页相关：**
- 排名计算逻辑是否依赖分页数据？如果是，必须改为全量计算或后端返回全局排名字段

**请求时序相关：**
- 切换筛选/Tab/分类是否可能引发请求竞态？慢请求是否可能在响应到达时覆盖快请求已经渲染好的结果？

**隐式依赖/缓存相关：**
- 读取缓存前，是否能说清楚缓存由谁写入、何时写入、失效后如何恢复
- 页面是否依赖其它页面先打开、先写缓存、先初始化状态
- 首次安装、首次进入、清缓存、扫码/分享直达时是否仍能正常工作
- 缓存缺失时，是否会重新请求接口、重新初始化或使用合理默认值

**生命周期相关：**
- 是否遗漏了 `onShow` 里的数据刷新（只在 `onLoad` 里请求了一次，返回/切 tab 后数据不会更新）？
- 新增的 timer、事件监听，是否在页面卸载（`onHide`/`onUnload`）时清理？

**跨页面通信相关：**
- 是否假设了固定的页面栈结构或固定的进入路径（`navigateTo`/EventChannel）？分享、扫码、直达场景下是否仍能正常工作？

**AI 生成代码常见模式：**
- 注释是否与实际逻辑一致？AI 改动逻辑后经常不同步更新注释，过时的注释会在后续 Review 时误导别人对代码行为的判断
- `||`/`??` 兜底的地方，是否确认了原始字段本身取值是否正确？不能因为最终展示结果看起来正常就直接放过——很可能是兜底值掩盖了原始字段一直没取对的问题
- 条件判断是否只写了 `if` 没写 `else`，隐式假设了另一个分支永远不会发生？需要明确这个假设是否真的成立
- 是否存在"复制粘贴变体"：同一段业务逻辑在两个页面/组件里各自实现了一遍，写法微微不同；这类代码后续任何一处修改都容易漏改另一处，出问题时也难以定位是哪个版本的逻辑在生效

**遗漏检查清单：**
- 是否误删了某个分类的特殊判断逻辑
- 是否遗漏了状态判断（如未判断"已取消"状态）
- 是否遗漏了权限校验（尤其是新增接口）
- 是否遗漏了空值/边界值保护
- 是否遗漏了时间限制逻辑（如活动未开始就能提交）
