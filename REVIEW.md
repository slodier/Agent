# 上线前检查（小程序 + 移动端 H5）

> AI 生成代码后必须逐项过一遍，不能只看改动点，要验证关联影响。

---

## 零、上线前确认（最高优先级，代码都不用看，先过这关）

> ⚠️ 这一类问题 AI 无法检查，也无法通过 Review 发现，根本原因是**需求信息不完整**就开始写代码。错的时间还能改，没确认的时间根本不知道该写什么。

**时间 / 配置类**
- 活动开始时间是否已由需求方明确确认（不能自己估一个）
- 活动结束时间是否已确认，且是最终版本（警惕中途改时间没同步到你）
- 按钮显示/隐藏的触发时间是否已确认，数据库里写的值是否和确认结果一致
- 所有写死在数据库/配置里的时间字段，上线前逐一核对一遍
- 如有时间改动，是否通知了所有相关方（后端/运营/测试）

**需求变更类**
- 本次上线前，是否有需求在开发过程中被口头修改过但未正式记录
- 群消息/口头沟通中的改动，是否已同步到代码和配置
- 有没有"以为对方改了，实际没改"的功能点（跨项目、跨端尤其要问清楚）

**未确认项卡控**
- 有任何未确认的配置/时间/规则时，**不允许上线**，必须先对齐

---

## 一、时间相关

- 活动开始/结束时间是否使用服务端时间，而非客户端本地时间
- 倒计时是否在页面切换后/回到前台后重新校准，而非继续原来的计时器
- 跨天场景：0 点前后状态切换是否正确（如"今天"变"昨天"、活动结束）
- 时区：后端返回的时间戳是否统一为 UTC，前端展示是否转换为本地时区
- 抢票/报名时间：是否有服务端二次校验，不能只靠前端禁用按钮拦截
- 直接访问活动路由（绕过入口）：页面初始化时是否主动拉取并校验活动状态，而非依赖上一页传参
- 时间限制的校验逻辑在**服务端**，前端路由拦截只是体验层，不是安全层

---

## 二、排行榜

> ⚠️ 真实 Bug：排行榜无分页时，每页数据独立计算排名，导致每页都出现第 1/2/3 名

- 排名是否基于**全量数据**计算，而非当前页数据
- 第二页第一条的排名是否接续第一页（不能重新从 1 开始）
- 前三名的特殊样式（金银铜牌）只能在**全局排名 1/2/3** 时出现，不随分页重置
- 分页参数 offset/page 是否正确传给接口，是否验证过第二页数据不重复
- 数据为空时：空状态 UI 是否正常，不能显示空列表或报错
- 数据量很大时：是否有分页或截断保护，避免一次性渲染卡顿

---

## 三、分类 / 类型扩展

> ⚠️ 真实 Bug：新增分类时 AI 将新分类的特殊逻辑（仅拍照、特殊表单提示）改进了公共逻辑，导致所有分类行为改变

- 新分类的特殊规则（上传限制、表单提示、审核流程）是否通过**分类 ID / 类型字段**做条件判断，而非直接修改公共逻辑
- 修改某一分类的逻辑后，其他分类的上传、表单、提示文案是否**未受影响**（必须实际点进去验证）
- 公共组件/公共函数是否被新增了只对特定分类生效的参数——这是危险信号，应优先用策略模式或配置隔离
- 新分类的限制（如仅允许拍照、不允许从相册选取）只在该分类下生效，旧分类行为不变
- 新分类的特殊提示文案在该分类下显示，旧分类仍显示原来的提示文案

---

## 四、权限

- 未登录用户：访问需登录页面是否跳转登录，而非白屏或报错
- 普通用户：是否能访问到管理员才能看的入口或接口（抓包验证，不能只看 UI 隐藏）
- 管理员：操作权限是否在服务端校验，不能只靠前端路由拦截
- 越权访问：修改请求参数中的 ID 是否能访问到他人数据（必须测试）
- 特殊角色（如审核员、运营）：权限边界是否正确，既不过宽也不过窄

---

## 五、活动状态

- 活动未开始：入口是否隐藏或禁用，点击是否有明确提示
- 活动进行中：所有功能正常，状态展示正确
- 活动已结束：入口是否隐藏，已参与数据是否仍可查看，不能直接 404
- 活动状态切换后：页面是否自动刷新或响应变化，不能停留在旧状态
- 活动状态由后端控制，前端不能绕过状态直接提交

---

## 六、分页

- 第一页数据是否正确，没有偏移错误
- 第二页是否有数据，且与第一页不重复
- 最后一页：没有"加载更多"按钮，或按钮状态正确禁用
- 总数展示是否与实际数据一致
- 空数据：空状态 UI 是否正常，不能显示"第 0 条/共 0 条"这类奇怪文案
- 下拉刷新后：页码是否重置为第一页

---

## 七、表单

- 必填项：为空提交是否有明确错误提示，提示定位到具体字段
- 选填项：为空提交后接口是否正常处理（不能传 null 导致后端报错）
- 默认值：是否在打开表单时正确填充，而非每次都是空
- 重复提交：按钮是否在提交后禁用，防止重复请求
- 提交成功后：是否有成功提示，页面/列表是否刷新
- 提交失败后：错误信息是否展示，按钮是否恢复可点击

---

## 八、上传（小程序重点）

- 图片上传：是否区分拍照和从相册选取，权限申请是否处理（用户拒绝后的降级）
- 视频上传：是否有时长限制、大小限制，超出时是否有提示
- 数量限制：达到上限后上传入口是否禁用，不能继续触发选择
- 格式限制：不支持的格式是否有明确提示，而非静默失败
- 删除后重新上传：旧文件是否正确清除，不能新旧混存
- 上传中断/失败：是否有重试机制或错误提示，不能卡在 loading 状态
- **新分类特殊限制**：仅拍照的分类，相册入口是否完全屏蔽（小程序 `sourceType` 只传 `['camera']`）

---

## 九、状态流转

- 各状态之间的流转路径是否与产品设计一致（如草稿→审核中→已发布）
- 不合法的状态跳转是否被拦截（如已取消的订单不能再支付）
- 状态切换后，页面上所有相关 UI（按钮、标签、提示）是否同步更新
- 状态由后端返回，前端不能本地缓存旧状态导致展示错误

---

## 十、小程序 / H5 特有

- 分享卡片：标题、图片、路径参数是否正确，带参数落地页是否正常
- 底部安全区：按钮/Tab Bar 是否适配 iPhone 底部横条（`safe-area-inset-bottom`）
- 键盘弹起：输入框是否被键盘遮挡，页面是否正确上推
- 长列表性能：超过 50 条数据是否有虚拟列表或分页，避免卡顿白屏
- 网络异常：断网/弱网下是否有提示，关键操作是否有重试
- H5 兼容性：iOS Safari 和 Android 微信内置浏览器是否都测过

---

## 十一、数据展示

- 列表页：空数据、单条数据、大量数据三种场景
- 详情页：所有字段是否有空值保护（字段缺失不能白屏或显示 undefined）
- 数字/金额：小数位、千分位格式是否正确
- 长文本：是否有截断处理，展开/收起是否正常
- 统计数字：汇总数据是否与列表数据吻合，不能对不上

---

## 十二、回归检查

- 本次改动的功能：核心路径全部走一遍
- 关联页面：入口页、上下游页面是否正常
- 关联接口：改动涉及的接口，其他调用方是否受影响
- 旧分类 / 旧功能：确认行为未变化（不能只看代码，要实际点击验证）
- **问题闭环确认**：之前发现的 bug 或讨论过的修改，是否亲自确认已上线生效——不能依赖"对方说改了"，跨项目/跨端的改动尤其要自己验证

---

## 十三、AI 代码 Review 重点（每次必查）

**改公共逻辑前必问：**
- 这个改动是否只对某一分类/类型生效？如果是，必须加条件判断，不能直接改公共代码
- 修改了公共组件/公共函数后，是否验证了**所有调用方**的表现，而非只测当前需求的入口

**排名/分页相关：**
- 排名计算逻辑是否依赖分页数据？如果是，必须改为全量计算或后端返回全局排名字段

**遗漏检查清单：**
- 是否误删了某个分类的特殊判断逻辑
- 是否遗漏了状态判断（如未判断"已取消"状态）
- 是否遗漏了权限校验（尤其是新增接口）
- 是否遗漏了空值/边界值保护
- 是否遗漏了时间限制逻辑（如活动未开始就能提交）
